<!-- source: https://github.com/ThariqS/html-effectiveness/blob/main/01-exploration-code-approaches.html -->
# Exploration as an HTML artifact

Different from `16-implementation-plan.html`. Not a sequenced build. One page: a prompt, then three approaches side by side.

## Shape

Title: "Debounced search — three approaches." Eyebrow + prompt box (the user ask). Grid of three `.approach` cards, each with a numbered heading and the design of that approach.

The spec *is* the comparison. Agent and human both open the HTML. No `docs/plan.md`.
