<!-- source: https://github.com/mattpocock/skills/blob/main/skills/engineering/to-spec/SKILL.md -->
# to-spec (Matt Pocock)

Synthesize the conversation into a spec. Do not interview. Publish to the issue tracker with `ready-for-agent`.

## Problem Statement

The problem from the user's perspective.

## Solution

The solution from the user's perspective.

## User Stories

A LONG numbered list:

1. As an <actor>, I want a <feature>, so that <benefit>

## Implementation Decisions

Modules, interfaces, schema, API contracts, architectural calls.

Do NOT include file paths or code snippets (they rot). Exception: a prototype snippet that *is* the decision (state machine, schema, type shape).

## Testing Decisions

- What a good test is (external behavior, not internals)
- Which modules get tests
- Prior art in the repo

## Out of Scope

## Further Notes
